<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Patterns" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.Patterns" parent="Base.Theme.Patterns" />


    <style name="Theme.Patterns.Transparent" parent="Theme.Patterns" >



        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:background">#12000000</item>
        <item name="android:colorBackgroundCacheHint">@null</item>
<!--        <item name="android:windowFrame">@color/transparent</item>-->
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowAnimationStyle">@android:style/Animation</item>

<!--        <item name="android:windowIsFloating">true</item>-->
    </style>

    <!--
       <style name="Theme.Patterns.Transparent" parent="Theme.Patterns" >

        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowCloseOnTouchOutside">false</item>
        <item name="android:background">@color/transparent</item>
    <item name="android:windowFrame">@null</item>

</style>
    -->
</resources>